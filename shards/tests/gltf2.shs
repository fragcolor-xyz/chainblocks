Sequence(models Type: @type([@type(GLTF.Read)]))

"data/kenney-space-station-kit" | FS.Iterate(Recursive: false)
ForEach({
  When({String.Ends(".glb")} {
    Log("Reading")
    GLTF.Read >> models
  })
})

models | Log("Models")


"File " | GLTF.Parse({})
; Output: 
ModelHierarchy {
  ; <nodes>
}

Node {
  ; <drawables>
}

Drawable {
  material: {
    id: "95376f49-7169-4363-8d8b-155b424a7eaf"
    t: 0
  }
}
