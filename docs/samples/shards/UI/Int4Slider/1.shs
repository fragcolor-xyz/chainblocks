GFX.MainWindow(Contents: {
  ; Setup
  Table(ui-draw-queue Type: GFX.DrawQueue)
  GFX.UIPass(ui-draw-queue) >> render-steps

  ; UI
  UI({
    UI.CentralPanel({
      UI.Int4Slider(
        Variable: int4
        Min: Int4(1 1 1 1)
        Max: Int4(100 100 100 100)
      ) | ExpectInt4
    })
  })

  GFX.Render(Steps: render-steps)
})
